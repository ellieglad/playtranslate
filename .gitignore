# Local build config (may contain API keys)
local.properties

# Build outputs
build/
app/build/
.gradle/

# Android SDK / JDK (bundled toolchain, not part of source)
.sdk/

# Claude Code
.claude/

# Local scratch / test data (not committed)
local/
scripts/thor-wifi-connect.sh
docs/

# Python cache from scripts/
__pycache__/
*.pyc

# Release APK (distributed via GitHub Releases, not committed)
*.apk

# Icon source images
img/

# Android Studio
.idea/
*.iml
.DS_Store

# Debug screenshots and session logs
dirty_*.png
ocr_*.png
session_*.jsonl

# OCR golden-set test data — local only.
# Screenshots are copyrighted game frames; transcribed expected.txt files
# are rebuilt locally via the seed-capture toggle (Settings → Debug →
# "Save OCR captures as seeds"). Test infrastructure (test class, recipes,
# .gitkeep) is committed; the data is not.
app/src/androidTest/assets/ocr_golden/**/*.png
app/src/androidTest/assets/ocr_golden/**/*.txt
app/src/androidTest/assets/ocr_golden/**/*.notes.md

# OCR investigation staging directories (pulled from device, not source).
/ocr-debug/
/ocr-seeds/
/ocr-golden-reports/

# OS
Thumbs.db
**/.cxx/
